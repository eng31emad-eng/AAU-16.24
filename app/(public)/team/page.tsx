import Team from './client';

export const metadata = {
  title: 'Team | Baghdad New University',
  description: 'Meet our distinguished team.',
};

export default function TeamPage() {
  return <Team />;
}
